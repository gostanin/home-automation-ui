import React from 'react';

import Cards from '../components/Cards/Cards';

const devices = (props) => {
    return (
        <div>
            <Cards />
        </div>
    );
}

export default devices;